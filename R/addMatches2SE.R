#' Function for adding results of a Matched Object to a Summarized Experiment
#'
#' @param mtches A vector of filepaths to matched Objects stored as *.RDS
#' @param se A SummarizedExperiment Object
#' 
#' @return SummarizedExperiment Object with added Annotations. For each Matched Object, a new List is implemented in RowData
#' 
addMatches2SE <- function(mtches, se){
    
    
    return(se)
}